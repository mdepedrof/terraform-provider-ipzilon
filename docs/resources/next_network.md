---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ipzilon_next_network Resource - ipzilon"
subcategory: ""
description: |-
  Atomically reserves the first available network block of a given prefix length inside a scope. The CIDR is assigned by the server.
---

# ipzilon_next_network (Resource)

Atomically reserves the first available network block of a given prefix length inside a scope. The CIDR is assigned by the server.

## Example Usage

```terraform
# Reserve the first available /24 in a scope
resource "ipzilon_next_network" "example" {
  scope_id      = ipzilon_scope.example.id
  prefix_length = 24
  name          = "landing-zone-network"
  description   = "Landing Zone Network"
}

# The assigned CIDR is available after apply:
# ipzilon_next_network.example.cidr → "10.0.1.0/24"
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `prefix_length` (Number) Desired prefix length (e.g. 24 for /24).
- `scope_id` (Number) Scope to allocate from.

### Optional

- `description` (String) Free-text description.
- `name` (String) Resource name.

### Read-Only

- `cidr` (String) Assigned CIDR block (computed by server).
- `id` (Number) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import ipzilon_next_network.example 1
```
