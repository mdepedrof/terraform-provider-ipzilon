---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "ipzilon_last_subnet Resource - terraform-provider-ipzilon"
subcategory: ""
description: |-
  Atomically reserves the last available subnet block of a given prefix length inside a network. The CIDR is assigned by the server.
---

# ipzilon_last_subnet (Resource)

Atomically reserves the last available subnet block of a given prefix length inside a network. The CIDR is assigned by the server.

## Example Usage

```terraform
# Reserve the last available /27 in a network (useful for management subnets)
resource "ipzilon_last_subnet" "mgmt" {
  network_id    = ipzilon_network.example.id
  prefix_length = 27
  name          = "mgmt-tier"
  description   = "Management Subnet"
}

# The assigned CIDR is available after apply:
# ipzilon_last_subnet.mgmt.cidr → "10.0.1.224/27"
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_id` (Number) Network to allocate from.
- `prefix_length` (Number) Desired prefix length (e.g. 27 for /27).

### Optional

- `description` (String) Free-text description.
- `name` (String) Resource name.

### Read-Only

- `cidr` (String) Assigned CIDR block (computed by server).
- `id` (Number) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import ipzilon_last_subnet.example 1
```
